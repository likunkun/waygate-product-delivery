# 2026-06-22 Open Spec Memory

| Field | Value |
| --- | --- |
| Topic | Product Delivery Agent Plugin roadmap Open Spec packages |
| Author | Codex |
| Status | V1.0 runtime implementation and multi-agent coverage review follow-up complete |

## Version Packages

- V0.1: `v0.1-roadmap-and-product-definition` - Complete the product shape, capability boundary, version route, and skill allocation for the Codex-native Product Delivery Agent Plugin.
- V0.2: `v0.2-artifact-and-state-protocol` - Define the local state and artifact protocol the plugin will use.
- V0.3: `v0.3-local-skill-workflow-prototype` - Validate the product delivery workflow with a repo or local skill before packaging a plugin.
- V0.4: `v0.4-skill-allocation-and-review-gates` - Explicitly include the Waygate README recommended skills in the workflow.
- V0.5: `v0.5-hooks-and-recovery-guardrails` - Preserve workflow continuity across compaction, resume, and long sessions.
- V0.6: `v0.6-ui-prototype-gate` - Provide local 1:1 HTML prototype confirmation for UI projects.
- V0.7: `v0.7-non-ui-behavior-contract-gate` - Provide a behavior confirmation gate for non-UI projects.
- V0.8: `v0.8-test-coverage-audit` - Confirm test obligations before implementation handoff.
- V0.9: `v0.9-codex-goal-handoff` - Generate a frozen version package that can be handed to implementation Codex.
- V0.10: `v0.10-feature-closure-gate` - Require formal closure gate and a version-specific closure artifact after implementation.
- V1.0: `v1.0-codex-plugin-packaging` - Package the stable workflow as an installable Codex plugin.

## Decisions

- Product shape is Codex-native Product Delivery Agent Plugin.
- Plugin remains dormant until explicit project-level `start`.
- UI projects use local 1:1 HTML prototype confirmation only when `project_type = ui`.
- Non-UI projects use behavior contract confirmation only when `project_type = non_ui`.
- Codex Goal is the first implementation handoff target.
- Direct Waygate state mutation is out of scope for V1.
- Feature Closure is an independent V0.10 package between Codex Goal handoff and plugin packaging.
- This fusion updates only `docs/open-spec/`; roadmap and workflow documents remain unchanged.
- V0.10 requires closure evidence without directly modifying Waygate or controller state.
- Multi-agent test coverage review completed for V0.1-V1.0 `06-test-cases.md` documents.
- All version test-case documents now include explicit FR/NFR/TASK coverage matrices.
- V0.8 now carries closure-ready fields including coverage status, exemption status, semantic marker, `latest_test_case`, and `matrix_range`.
- V0.10 now includes negative artifact validation for missing or invalid required fields, command evidence, E2E coverage, integrity booleans, scope guard, and CR supersession.
- V1.0 now requires `.codex-plugin/plugin.json` and future runtime verification that install/start/stop/upgrade flows do not mutate Waygate/controller state.
- V0.2 runtime implementation is complete in `src/product_delivery_agent/artifact_protocol.py`.
- V0.2 tests are in `tests/test_artifact_protocol.py`; `PYTHONPATH=src python3 -m unittest tests/test_artifact_protocol.py` passed with 5 tests.
- V0.3 should build lifecycle workflow commands on top of the V0.2 artifact/state helpers rather than duplicating state handling.
- V0.3 runtime implementation is complete in `src/product_delivery_agent/workflow.py`.
- V0.3 tests are in `tests/test_workflow_prototype.py`; `PYTHONPATH=src python3 -m unittest tests/test_workflow_prototype.py` passed with 6 tests.
- Combined V0.2+V0.3 tests passed with `PYTHONPATH=src python3 -m unittest discover -s tests` running 11 tests.
- V0.4 should attach explicit skill allocation and review gates to the V0.3 workflow surface.
- V0.4 runtime implementation is complete in `src/product_delivery_agent/skill_gates.py` and `ProductDeliveryWorkflow.record_skill_use`.
- V0.4 tests are in `tests/test_skill_gates.py`; `PYTHONPATH=src python3 -m unittest tests/test_skill_gates.py` passed with 6 tests.
- Combined V0.2-V0.4 tests passed with `PYTHONPATH=src python3 -m unittest discover -s tests` running 17 tests.
- V0.5 should use existing disk state and `skill_records` as hook/recovery context inputs.
- V0.5 runtime implementation is complete in `src/product_delivery_agent/hooks.py`.
- V0.5 tests are in `tests/test_hooks_recovery.py`; current `PYTHONPATH=src python3 -m unittest tests/test_hooks_recovery.py` passed with 7 tests after multi-agent coverage follow-up.
- Current full-suite tests passed with `PYTHONPATH=src python3 -m unittest discover -s tests` running 69 tests.
- V0.5 hook helpers are pure local context/guardrail builders and do not mutate Waygate or controller state.
- A read-only V0.5 coverage review subagent disconnected with a transport error before completion; mainline TDD and full-suite verification remain the recorded evidence.
- V0.6 should implement the UI-only prototype gate on top of V0.3 project type routing and V0.5 guardrail/state behavior.
- V0.6 runtime implementation is complete in `src/product_delivery_agent/ui_prototype.py` and `ProductDeliveryWorkflow.record_ui_prototype_review`.
- V0.6 tests are in `tests/test_ui_prototype_gate.py`; current `PYTHONPATH=src python3 -m unittest tests/test_ui_prototype_gate.py` passed with 6 tests after multi-agent coverage follow-up.
- Current full-suite tests passed with `PYTHONPATH=src python3 -m unittest discover -s tests` running 69 tests.
- V0.6 records UI-only prototype review state, required UI scenario taxonomy, downstream browser E2E candidates, negative scope guard candidates, and prototype limitations for later audit/handoff/closure.
- V0.7 should implement the non-UI behavior contract gate as the mutually exclusive branch for `project_type = non_ui`.
- V0.7 runtime implementation is complete in `src/product_delivery_agent/non_ui_behavior.py` and `ProductDeliveryWorkflow.record_non_ui_behavior_contract`.
- V0.7 tests are in `tests/test_non_ui_behavior_contract.py`; current `PYTHONPATH=src python3 -m unittest tests/test_non_ui_behavior_contract.py` passed with 6 tests after multi-agent coverage follow-up.
- Current full-suite tests passed with `PYTHONPATH=src python3 -m unittest discover -s tests` running 69 tests.
- V0.7 records non-UI behavior contract state, required non-UI scenario taxonomy, downstream behavior evidence candidates, negative boundary candidates, and behavior limitations for later audit/handoff/closure.
- V0.8 should implement coverage audit over both UI downstream inputs (`browser_e2e_candidates`, negative scope guards) and non-UI downstream inputs (`behavior_evidence_candidates`, negative boundary candidates).
- V0.8 runtime implementation is complete in `src/product_delivery_agent/coverage_audit.py` and `ProductDeliveryWorkflow.record_test_coverage_audit`.
- V0.8 tests are in `tests/test_coverage_audit.py`; `PYTHONPATH=src python3 -m unittest tests/test_coverage_audit.py` passed with 9 tests.
- Current full-suite tests passed with `PYTHONPATH=src python3 -m unittest discover -s tests` running 69 tests.
- V0.8 validates continuous `TC-V008-*` ranges, `FR/NFR/US/J/AC/TASK` anchors, semantic markers, critical gap status, UI browser E2E obligations, non-UI behavior evidence obligations, supporting evidence classification, inherited negative guards, inherited limitations, `matrix_range`, and `latest_test_case`.
- V0.9 should generate Codex Goal handoff from `test_coverage_audit`, `handoff_inputs`, `closure_inputs`, and frozen scope data without executing implementation tests.
- V0.9 runtime implementation is complete in `src/product_delivery_agent/handoff.py` and `ProductDeliveryWorkflow.generate_codex_goal_handoff`.
- V0.9 tests are in `tests/test_codex_goal_handoff.py`; current `PYTHONPATH=src python3 -m unittest tests/test_codex_goal_handoff.py` passed with 6 tests after multi-agent coverage follow-up.
- Current full-suite tests passed with `PYTHONPATH=src python3 -m unittest discover -s tests` running 69 tests.
- V0.9 generates `handoff.md` and `codex-goal-prompt.md`, freezes scope, carries coverage/obligation/command/negative guard data, records post-freeze CR changes, and links superseded closures to triggering CRs.
- V0.10 should validate the post-implementation formal closure artifact against V0.8/V0.9 closure readiness inputs without treating chat summaries or progress notes as closure evidence.
- V0.10 runtime implementation is complete in `src/product_delivery_agent/closure.py` and `ProductDeliveryWorkflow.record_feature_closure`.
- V0.10 tests are in `tests/test_feature_closure.py`; current `PYTHONPATH=src python3 -m unittest tests/test_feature_closure.py` passed with 12 tests after multi-agent coverage follow-up.
- Current full-suite tests passed with `PYTHONPATH=src python3 -m unittest discover -s tests` running 69 tests.
- V0.10 validates version-specific closure artifacts, rejects summary-only evidence, requires E2E/user-story/journey coverage fields, high-risk subresults, negative scope guard pass, command output, safe integrity booleans, matching `matrix_range`/`latest_test_case`, and CR links for superseded closures.
- V1.0 runtime implementation is complete in `src/product_delivery_agent/plugin_packaging.py`.
- V1.0 generated the repo-local plugin package under `plugins/product-delivery-agent/` and marketplace config under `.agents/plugins/marketplace.json`.
- V1.0 tests are in `tests/test_plugin_packaging.py`; `PYTHONPATH=src python3 -m unittest tests/test_plugin_packaging.py` passed with 6 tests.
- Multi-agent coverage review follow-up added missing test-case coverage for V0.1 main flow, V0.5 recovery branches, V0.6/V0.7 permissions and long tasks, V0.9 post-freeze CR/supersession/readiness, V0.10 closure metadata and high-risk negatives, and V1.0 closure template metadata.
- Current full-suite verification passed with `PYTHONPATH=src python3 -m unittest discover -s tests` running 69 tests.
- Python compile verification passed with `python3 -m py_compile src/product_delivery_agent/*.py`.
- Plugin validation passed with `python3 /home/lichangkun/.codex/skills/.system/plugin-creator/scripts/validate_plugin.py plugins/product-delivery-agent`.
